# Handover — 2026-03-01

## Current State

`scpn-quantum-control` is now aligned with:

1. orchestrator-defined phase/control contracts,
2. latest `scpn-control` plasma-native Knm interface.

Key integration commit:

- `143ebdd` on `main` (pushed).

## What Is Ready

- Shared UPDE artifact schema for cross-backend exchange.
- Orchestrator adapter for ingestion/export of phase state payloads.
- Plasma Knm compatibility bridge to consume:
  - `build_knm_plasma`
  - `build_knm_plasma_from_config`
  - `plasma_omega`
- Parity tests guarding drift against both:
  - `scpn-control` builders,
  - orchestrator base coupling contract.

## Operational Interfaces

### Orchestrator path

- `PhaseOrchestratorAdapter.from_orchestrator_state(...)`
- `PhaseOrchestratorAdapter.build_knm_from_binding_spec(...)`
- `PhaseOrchestratorAdapter.build_omega_from_binding_spec(...)`

### scpn-control plasma path

- `build_knm_plasma(..., repo_src=...)`
- `build_knm_plasma_from_config(..., repo_src=...)`
- `plasma_omega(..., repo_src=...)`

`repo_src` allows deterministic local multi-repo integration (no pip install required).

## Risks / Watch Items

1. API drift in `scpn-control.phase.plasma_knm` function signatures.
2. semantic changes in coupling constants or mode logic without corresponding test baseline updates.
3. optional-import environments where `scpn-control` is unavailable.

Mitigation already in place:

- parity tests fail fast on incompatible changes,
- lazy import error message points to remediation.

## Recommended Next Actions

1. Add CI matrix job that runs parity tests with local `../scpn-control/src` when available.
2. Add an end-to-end notebook:
   - fusion binding spec -> bridge -> XY Hamiltonian -> quantum run.
3. Add versioned compatibility table in docs:
   - tested `scpn-control` commit hash vs `scpn-quantum-control` commit hash.

## Quick Re-run Commands

```bash
cd 03_CODE/scpn-quantum-control
ruff check src/ tests/
pytest tests/test_knm_parity.py tests/test_orchestrator_adapter.py tests/test_phase_artifact.py -q
```

